.container
  .row
    .col-md-12
      %h1 All Surveys
  .row
    .col-md-6
      .panel.panel-default
        .panel-heading
          %h4 Upload Results File
        .panel-body.file-upload
          = form_tag '/survey_results', multipart: true do
            %span.btn.btn-default.btn-file.file-upload-proxy
              Browse for file
            %input{ type: 'file', class: 'hidden', name: 'file' }
            %input{ type: 'submit', class: "btn btn-primary pull-right" }

      %ul.files.list-group
        - @surveys.each do |survey|
          %li.list-group-item.clearfix
            = survey.name
            .btn-group.pull-right
              %a{ href: survey_result_path(survey), class: "btn btn-default"}
                View Results
                .glyphicon.glyphicon-stats{"aria-hidden" => "true"}
              = link_to survey_result_path(survey), method: :delete, class: "btn btn-danger" do
                .glyphicon.glyphicon-remove{"aria-hidden" => "true"}
        